{
  "name": "foclock",
  "private": true,
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && npx html-minifier-terser src/index.html --collapse-whitespace --remove-comments --minify-css true --minify-js true -o dist/index.html && npx html-minifier-terser src/live.html --collapse-whitespace --remove-comments --minify-css true --minify-js true -o dist/live.html"
  }
}
